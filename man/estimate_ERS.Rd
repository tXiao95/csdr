% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_ERS.R
\name{estimate_ERS}
\alias{estimate_ERS}
\title{Estimate Causal Mean using RA, IPW, or DR (Self-Normalized) from Colangelo and Lee (2026)}
\usage{
estimate_ERS(
  Y,
  X,
  C,
  x_eval = NULL,
  estimator = c("DR", "RA", "IPW"),
  out_model = NULL,
  gps_model = NULL,
  h = NULL,
  c_multiplier = 1.25,
  delta_n = 1e-16,
  optimize_bw = FALSE,
  return_vector = FALSE
)
}
\arguments{
\item{Y}{Numeric vector of observed outcomes (length n).}

\item{X}{Numeric matrix or data frame of observed treatments (n x p).}

\item{C}{Numeric matrix or data frame of observed confounders (n x q).}

\item{x_eval}{Matrix or data frame of target treatment values to evaluate (m x p).}

\item{estimator}{String indicating the estimator to use: "DR", "RA", or "IPW". Defaults to "DR".}

\item{out_model}{An S3 object of class "outcome_model" for the outcome regression. Required for RA and DR.}

\item{gps_model}{An S3 object of class "gps_model" for the GPS. Required for IPW and DR.}

\item{h}{Optional numeric vector of bandwidths (length p). Defaults to rule-of-thumb.}

\item{c_multiplier}{Numeric scalar for rule-of-thumb bandwidth calculation. Defaults to 1.25.}

\item{delta_n}{Small positive threshold to floor propensity scores. Defaults to 1e-4.}

\item{optimize_bw}{Logical. If TRUE and estimator is "DR", dynamically calculates the AMSE-optimal
bandwidth using pilot estimates, undersmooths it, and re-estimates. Defaults to FALSE.}

\item{return_vector}{Logical. If TRUE, forces DR to return only a numeric vector of point estimates.
RA and IPW always return a numeric vector regardless of this setting. Defaults to FALSE.}
}
\value{
A list containing a 'results' data frame (estimates and CIs) and 'metadata' (diagnostics),
OR a numeric vector of estimates if return_vector is TRUE or estimator is RA/IPW.
}
\description{
Estimate Causal Mean using RA, IPW, or DR (Self-Normalized) from Colangelo and Lee (2026)
}
