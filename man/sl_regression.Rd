% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learners.R
\name{sl_regression}
\alias{sl_regression}
\alias{mvn_gps}
\alias{custom_regression}
\alias{custom_gps}
\title{Construct CSDR learner specifications}
\usage{
sl_regression(
  SL.library = csdr_default_sl_library(),
  family = stats::gaussian(),
  ...
)

mvn_gps(method = c("linear", "SuperLearner"), density_floor = 1e-16, ...)

custom_regression(fitter, args = list(), label = NULL)

custom_gps(fitter, args = list(), label = NULL)
}
\arguments{
\item{SL.library}{Character vector of SuperLearner wrapper names.}

\item{family}{Regression family passed to \code{\link[SuperLearner:SuperLearner]{SuperLearner::SuperLearner()}}.}

\item{...}{Additional arguments stored for the low-level fitter.}

\item{method}{Conditional mean model for the multivariate normal GPS.}

\item{density_floor}{Positive lower bound for predicted densities.}

\item{fitter}{User-supplied fitting function.}

\item{args}{Additional arguments stored for the fitter.}

\item{label}{Optional human-readable learner label.}
}
\value{
A CSDR learner specification object.
}
\description{
Construct CSDR learner specifications
}
\details{
\code{custom_regression()} expects \code{fitter(Y, W, ...)} to return a fitted object
with a working \code{predict(object, newdata = ...)} method. \code{custom_gps()}
expects \code{fitter(A, C, ...)} to return a fitted density object whose
\code{predict(object, newdata = ...)} method returns numeric conditional density
estimates.
}
