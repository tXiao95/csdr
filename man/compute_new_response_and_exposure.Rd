% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_new_response_and_exposure.R
\name{compute_new_response_and_exposure}
\alias{compute_new_response_and_exposure}
\title{Compute New Response and Exposure for Causal SDR via Cross-Fitting}
\usage{
compute_new_response_and_exposure(
  Y,
  X,
  C,
  method = c("RA"),
  L = 5,
  folds = NULL,
  outcome_fitter = SL_outcome_fitter,
  C_fitter = SL_nuisance_fitter,
  gps_fitter = mvn_fitter,
  seed = 42,
  args_outcome = list(),
  args_C = list(),
  args_gps = list(),
  args_ers = list(),
  po_marginalization = NULL,
  verbose = TRUE
)
}
\arguments{
\item{Y}{Numeric vector of outcomes (length n).}

\item{X}{Numeric matrix or data frame of observed treatments (n x p).}

\item{C}{Numeric matrix or data frame of observed confounders (n x q).}

\item{method}{String or vector indicating methods to use (e.g., "DR" or c("RA", "DR", "PO", "RP")).}

\item{L}{Integer indicating the number of folds for cross-fitting. Defaults to 5.}

\item{folds}{Optional fold assignments.}

\item{outcome_fitter}{Function to train outcome models. Required for RA, DR, PO.}

\item{C_fitter}{Function to train nuisance models. Required for RP.}

\item{gps_fitter}{Function to train GPS models. Defaults to mvn_fitter. Required for DR, PO.}

\item{seed}{Random seed for cross-fitting folds.}

\item{args_outcome}{List of additional arguments to pass to outcome_fitter.}

\item{args_C}{List of additional arguments to pass to C_fitter.}

\item{args_gps}{List of additional arguments to pass to gps_fitter (e.g., list(method="linear")).}

\item{args_ers}{List of additional arguments to pass to estimate_ERS (e.g., list(optimize_bw=TRUE)).}

\item{po_marginalization}{Marginalization mode for PO targets. Defaults to
\code{"crossfit"} through \code{\link[=csdr_target]{csdr_target()}}.}

\item{verbose}{Logical; if \code{TRUE}, print progress messages.}
}
\value{
A list containing \code{new_Y}, \code{new_X}, and \code{metadata}.
}
\description{
Compute New Response and Exposure for Causal SDR via Cross-Fitting
}
