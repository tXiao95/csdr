% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ers_estimate.R
\name{estimate_ers}
\alias{estimate_ers}
\title{Estimate a causal exposure-response surface}
\usage{
estimate_ers(
  Y,
  A,
  C,
  a_eval = NULL,
  estimator = c("DR", "RA", "IPW"),
  L = 5,
  folds = NULL,
  outcome_fitter = SL_outcome_fitter,
  gps_fitter = mvn_fitter,
  h = NULL,
  c_multiplier = 1.25,
  gps_floor = 1e-08,
  normalize_ipw = TRUE,
  seed = NULL,
  return_nuisance = FALSE,
  verbose = TRUE,
  args_outcome = list(),
  args_gps = list()
)
}
\arguments{
\item{Y}{Numeric outcome vector of length \code{n}.}

\item{A}{Numeric matrix or data frame of observed exposures/treatments
with \code{n} rows.}

\item{C}{Numeric matrix or data frame of covariates with \code{n} rows.}

\item{a_eval}{Optional matrix or data frame of exposure values where the
exposure-response surface should be evaluated. Defaults to the observed
rows of \code{A}.}

\item{estimator}{Estimator to use: \code{"DR"}, \code{"RA"}, or \code{"IPW"}.}

\item{L}{Number of folds for cross-fitting. \code{L = 1} disables cross-fitting
and fits nuisances on the full data.}

\item{folds}{Optional fold assignments of length \code{n}. If supplied, these
are used instead of randomly generated folds.}

\item{outcome_fitter}{Function used by \code{\link[=outcome_model]{outcome_model()}} to fit
\eqn{E[Y \mid A, C]}. Required for \code{"RA"} and \code{"DR"}.}

\item{gps_fitter}{Function used by \code{\link[=gps_model]{gps_model()}} to fit the conditional
exposure density \eqn{f(A \mid C)}. Required for \code{"IPW"} and \code{"DR"}.}

\item{h}{Optional numeric bandwidth vector. If \code{NULL}, defaults to
\code{c_multiplier * apply(A, 2, sd) * n^(-0.2)}.}

\item{c_multiplier}{Multiplier used for the default bandwidth.}

\item{gps_floor}{Positive floor applied to GPS estimates.}

\item{normalize_ipw}{Logical; if \code{TRUE}, use self-normalized IPW weights.}

\item{seed}{Optional random seed used when \code{folds} is not supplied.}

\item{return_nuisance}{Logical; if \code{TRUE}, return fitted nuisance objects
and out-of-fold predictions.}

\item{verbose}{Logical; if \code{TRUE}, print progress messages.}

\item{args_outcome}{Additional arguments passed to \code{outcome_fitter}.}

\item{args_gps}{Additional arguments passed to \code{gps_fitter}.}
}
\value{
An S3 object of class \code{"ers_fit"}.
}
\description{
\code{estimate_ers()} estimates the causal exposure-response surface
\eqn{\mu(a) = E[Y^a]} for a continuous, possibly multivariate exposure.
It supports regression adjustment (\code{"RA"}), inverse probability weighting
(\code{"IPW"}), and doubly robust (\code{"DR"}) estimators. Nuisance models are fit
either on the full data (\code{L = 1}) or by cross-fitting (\code{L > 1}).
}
